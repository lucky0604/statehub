# packages/ui

Reserved for shared UI primitives extracted from `apps/web` when reuse across apps justifies a package. Not used at P00.

shadcn/ui components live in `apps/web/src/components/ui` at P00 (colocated with the only consumer).
